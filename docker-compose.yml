services:
  slimebot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slimebot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HOME: /var/lib/slimebot/codex
    volumes:
      - ./workspace:/var/lib/slimebot/workspace
      - ./codex:/var/lib/slimebot/codex

volumes:
  codex-data:
